{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "player", "twitter-auth"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
